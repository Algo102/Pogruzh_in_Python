l1 = [4, 8, 2, 9, 1, 7, 2]

# Выдает новый сортированный список
# Работает с: списками, кортежами, множествами и т.д.
sort_l = sorted(l1)
print(l1, sort_l, sep='\n')

# В обратном порядке
rev_l = sorted(l1, reverse=True)
print(l1, rev_l, sep='\n')
